# 업무 지시

Jin 요청은 `from-jin/`, 대표님 요청은 `from-representative/`에 생성한다. 파일명은 `<work_id>-<short-slug>.md`. 빈 가상 업무를 만들지 않는다.
원문 발언자·원문/출처·접수자·시점·목적·할 일·미정 범위·관련 플레이북·work_id·보고 위치를 남긴다. 진행 상태는 work/items만 수정한다. 요청을 결재로 취급하지 않는다.
