# Jin 역할 — GOV-20260906-05

- actor_id: `ACT-001`
- role: `admin`
- repository: `jintonic1010/miracle_company_private`
- branch: `main`

이 프로젝트는 미라클 회사 관리자 Jin의 업무·품의 최종 결재 프로젝트다. actor_id는 `ACT-001`, 역할은 `llm-source/actors/ACT-001/ROLE.md`의 admin으로 고정한다. 이 운영체계의 최종 결재자는 Jin이다.

업무요청은 `/업무결정 <승인|수정요청|보류|반려>`, 품의서는 `/품의서결정 <승인|수정요청|보류|반려>`로 결정한다. 대상·버전·범위와 실제 이유·조건을 남긴다. 결과값을 생략하면 승인하지 않는다. 업무 시작을 명시한 승인만 W-ID를 생성/연결하며 중복 업무를 만들지 않는다. 결과 수락은 /결과승인, 회사 기준 변경은 /기준확정이다. /업무점검 대표님·부사수·전체로 현황을 조회한다.
내 기존 회사 요청이 의미 있게 변경되면 R-ID·최초 원문·revision/supersedes를 보존하고 RC-ID에 변경 전후·이유·근거·영향 W/D를 남긴다. 이유 미확인은 null/unknown이며 추측하지 않는다. 표현 수정·재확인·읽기 전용에는 RC를 만들지 않는다. 실제 저장은 최신 REQUEST_CHANGE_WORKFLOW를 따른다.
새 사람에게는 새 ACT-ID를 발급한다. 내가 범위가 명확한 검토·반영을 요청하면 실제 도구로 순차 처리하며 불필요하게 같은 승인을 다시 묻지 않는다. 일반 대화는 승인으로 바꾸지 않는다.

## 허용 명령과 로컬 추천

`/아이디어출력` `/아이디어저장` `/문제출력` `/문제저장` `/업무검토` `/업무요청` `/업무업데이트` `/품의서작성` `/업무결정` `/품의서결정` `/업무점검` `/결과승인` `/반영미리보기` `/기준확정` `/아이디어반영` `/문제반영`

PROJECT_COMMON의 각 명령 용도·추천 조건을 GitHub 없이도 적용한다. 자기 역할의 다음 동작 하나만 제안하고 반복하지 않는다. 실제 실행은 최신 FILE_MAP·ACTOR_REGISTRY·LLM_RUNTIME·해당 workflow를 확인한다. 로컬 출력·읽기 전용은 쓰지 않는다. 명시적 수행·지속 요청만 원문 보존한다.

## 기록·권한·알림

자기 actor_id의 아이디어/문제/요청을 사용한다. 원 발언자·작성자·수행자·결재자는 따로 남긴다. 과거 role 경로와 actor_id를 바꾸지 않는다. 업무 원장은 work/items, 결정은 approvals, 상태함·INBOX는 파생 조회판이다.
새 업무 대화와 관련 건 재개 시 자기 INBOX만 선택 조회한다. 새 결정 변화는 한번 안내하되 표시·확인을 혼동하지 않는다. 읽기 전용에서는 영속 전달 기록을 쓰지 않는다. 같은 건 검토에는 관련 과거 결정이 여전히 유효한지 확인한다.
공통 업무 접근은 타인의 인사·개인화·비공개 자료에 대한 무제한 권한이 아니다. 비밀·개인정보를 GitHub에 저장하지 않는다. 실제 계정 권한을 넘지 않는다. 승인 출처를 확인하고 역할 기반 확인을 독립 계정 인증으로 과장하지 않는다. 실제 커밋·내용 재조회 전 완료라고 말하지 않는다.
